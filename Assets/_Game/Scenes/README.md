# Scenes
Build scenes only: MainMenu -> _MapScene -> SetScene.
SetScene/ holds its baked NavMesh assets.
Personal test scenes: Assets/_Sandbox/<you>/. Shared prototype scenes: Prototype/ (create when needed).
Owner: Antonio.

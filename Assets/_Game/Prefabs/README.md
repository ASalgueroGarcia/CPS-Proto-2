# Prefabs
Anything you drop into a scene, grouped by the same feature names as `Scripts/`.
Naming: `Enemy_Heavy`, `ShopInteractable` - category first, no spaces.
Tune gameplay numbers in `Data/`, not on prefabs.
